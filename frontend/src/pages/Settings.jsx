import {useEffect,useState} from 'react'; import {api} from '../services/api';
export default function Settings(){const [form,setForm]=useState(null),[message,setMessage]=useState('');useEffect(()=>{api.settings().then(setForm).catch(e=>setMessage(e.message))},[]);if(!form)return <p>Loading settings…</p>;return <section className="panel"><h2>Detection rules</h2>{[['loitering_threshold','Loitering seconds'],['crowd_threshold','Crowd people count'],['abandoned_object_threshold','Stationary-object seconds']].map(([key,label])=><label key={key}>{label}<input type="number" value={form[key]} onChange={e=>setForm({...form,[key]:Number(e.target.value)})}/></label>)}<button className="primary" onClick={()=>api.saveSettings(form).then(()=>setMessage('Saved. Restart camera processing for rule changes to take effect.')).catch(e=>setMessage(e.message))}>Save settings</button><p>{message}</p></section>}
