import {BarChart,Bar,XAxis,YAxis,Tooltip,ResponsiveContainer} from 'recharts'; export default function Analytics({events}){const data=Object.entries(events.reduce((a,e)=>({...a,[e.event_type]:(a[e.event_type]||0)+1}),{})).map(([name,count])=>({name,count}));return <section className="panel"><h2>Event type distribution</h2>{data.length?<ResponsiveContainer width="100%" height={300}><BarChart data={data}><XAxis dataKey="name"/><YAxis/><Tooltip/><Bar dataKey="count" fill="#42d3a5"/></BarChart></ResponsiveContainer>:<p>Charts appear after real events are recorded.</p>}</section>}
