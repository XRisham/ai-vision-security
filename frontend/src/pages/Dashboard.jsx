import StatCard from '../components/StatCard'; import LiveMonitor from '../components/LiveMonitor'; import EventFeed from '../components/EventFeed';
export default function Dashboard({live,events}){return <><div className="stats"><StatCard label="PEOPLE DETECTED" value={live?.people_count}/><StatCard label="OBJECTS DETECTED" value={live?.object_count}/><StatCard label="ACTIVE EVENTS" value={events.length}/><StatCard label="CURRENT FPS" value={live?.fps}/><StatCard label="CAMERA STATUS" value={live?.camera_active?'ONLINE':'OFFLINE'}/></div><div className="grid"><LiveMonitor active={live?.camera_active}/><EventFeed events={events.slice(0,6)}/></div></>}
