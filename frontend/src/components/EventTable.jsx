export default function EventTable({events=[]}){return <section className="panel"><h2>Event History</h2><div className="table"><div className="row head"><span>Time</span><span>Event</span><span>Severity</span><span>Track</span><span>Zone</span></div>{events.map(e=><div className="row" key={e.id}><span>{new Date(e.timestamp).toLocaleString()}</span><span>{e.event_type}</span><span>{e.severity}</span><span>{e.track_id??'—'}</span><span>{e.zone??'—'}</span></div>)}</div></section>}
